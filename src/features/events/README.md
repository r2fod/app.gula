# Events Feature

Contains all logic and components related to Event management.

## Structure
- `components/`: UI components (Timeline, EventHeader, etc.)
- `hooks/`: Business logic (useEvents, useBeverages, etc.)
